# Cube Validator - Development Dockerfile
# Runs Angular dev server with hot-reload support

FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm ci

# Copy source files
COPY . .

# Expose Angular dev server port
EXPOSE 80

# Run ng serve with host binding for Docker
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--port", "80", "--poll", "2000", "--disable-host-check"]
