name: Build and Deploy to TEST

on:
  push:
    branches:
      - main-graphdb
  pull_request:
    branches:
      - main-graphdb
      - develop-graphdb

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: swissfederalarchives/lindas-cube-validator

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version from package.json
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Generate tags
        id: tags
        run: |
          SHA_SHORT=$(git rev-parse --short HEAD)
          echo "sha_tag=sha-${SHA_SHORT}" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.tags.outputs.sha_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save current TEST as previous
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          if docker pull $REGISTRY/$IMAGE_NAME:graphdb-test 2>/dev/null; then
            echo "Saving current 'graphdb-test' as 'graphdb-test-previous'"
            docker tag $REGISTRY/$IMAGE_NAME:graphdb-test $REGISTRY/$IMAGE_NAME:graphdb-test-previous
            docker push $REGISTRY/$IMAGE_NAME:graphdb-test-previous
          else
            echo "No existing 'graphdb-test' tag to save"
          fi

      - name: Deploy to TEST
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          docker pull $REGISTRY/$IMAGE_NAME:${{ steps.version.outputs.version }}
          docker tag $REGISTRY/$IMAGE_NAME:${{ steps.version.outputs.version }} $REGISTRY/$IMAGE_NAME:graphdb-test
          docker push $REGISTRY/$IMAGE_NAME:graphdb-test
          echo "Deployed ${{ steps.version.outputs.version }} to TEST"

      - name: Summary
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          echo "## Built and Deployed to TEST" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tag | Description |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|-------------|" >> $GITHUB_STEP_SUMMARY
          echo "| \`${{ steps.version.outputs.version }}\` | Version tag (immutable) |" >> $GITHUB_STEP_SUMMARY
          echo "| \`${{ steps.tags.outputs.sha_tag }}\` | SHA tag (immutable) |" >> $GITHUB_STEP_SUMMARY
          echo "| \`graphdb-test\` | TEST environment |" >> $GITHUB_STEP_SUMMARY
          echo "| \`graphdb-test-previous\` | Previous TEST version |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "To promote: Run **Deploy to INT** workflow with version \`${{ steps.version.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
